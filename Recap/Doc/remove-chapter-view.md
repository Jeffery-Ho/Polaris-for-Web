# 移除章节视图

## 原因

章节视图是用于全文阅读与复制的附加能力，不属于 marker 导航的核心流程；在 Kimi 页面存在遮罩误显示与关闭状态失效的兼容风险。

## 调整

- 控制胶囊仅保留“导航”和“设置”。
- 移除章节入口及 `Cmd/Ctrl+Shift+F` 快捷键。
- 渲染过程不再创建章节遮罩，并清理已存在的旧遮罩节点。

## 影响

保留 marker 生成、跳转、导航搜索、折叠与设置功能；不再提供章节阅读、章节搜索和复制全文能力。
