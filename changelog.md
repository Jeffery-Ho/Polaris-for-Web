# Changelog

## 2026-09-01

- 修复：长会话中的宿主 DOM 刷新不再重复重建设置面板；真实设置更新会保留面板内部滚动位置，底部 Marker 筛选项可正常访问。
- 版本：修订更新到 `0.44.1(185)`。

- 调整：用户 Maker 分组和 Maker 数量堆叠均改为默认展开；保留手动折叠，滚动和 Active Maker 定位不再自动改写分组状态。
- 版本：功能更新到 `0.44.0(184)`。

- 修复：Maker 列表滚动及其阴影缓冲层固定在搜索栏下方，不再遮挡或拦截搜索框。
- 版本：修订更新到 `0.43.3(183)`。

- 调整：Maker 改为只根据当前挂载的用户与 AI DOM 实时生成；路由切换立即移除旧列表，DOM 卸载后对应 Maker 立即消失，不再跨刷新、会话或 DOM 重挂保留历史。
- 调整：删除 Maker 快照、ChatGPT 会话 API 分支读取、稳定身份映射及缺失 DOM 主动恢复跳转；首次升级仅清理旧 `polaris.makerSnapshot.v1/v2` 命名空间并写入无对话内容的完成标记。
- 版本：修订更新到 `0.43.2(182)`。

- 修复：ChatGPT 切换会话时立即清空旧 Maker 队列；仅当前路由确认后的会话分支与已挂载 AI 回复可恢复 Maker，延迟回调和旧 DOM 不再回写。
- 修复：当前会话 AI 容器保留其用户分组归属；过期 assistant ID 不会生成 Maker，展开用户分组后可显示当前 AI Maker。
- 调整：移除无实际作用的“获取历史 Maker”入口、扫描、滚动锁、文案及相关样式；已有本地快照、早期问题展开和缺失 DOM 跳转恢复保持不变。
- 版本：修订更新到 `0.43.1(181)`。

## 2026-08-28

- 功能：六个平台的 Maker 列表新增独立“获取历史 Maker”卡片；ChatGPT 先刷新完整活跃分支，随后与其他平台共用受控 DOM 向前扫描，将新发现记录继续交给现有快照模型协调。
- 交互：单次扫描最多 10 秒，扫描期间锁定正文与 Maker 导航并保留取消入口；完成、超时或取消后优先按可见消息锚点恢复阅读位置，锚点失效时按距底部距离回退。
- 稳定性：到达历史起点并连续三轮无新增后结束，停滞、接口失败、路由切换及扩展上下文失效均会安全退出；ChatGPT 接口失败时继续 DOM 扫描并标记为部分结果。
- 版本：功能更新到 `0.43.0(180)`。

- 修复：所有用户 Maker 分组改为初始折叠，最新组、上一组及流式新增组不再自动展开；手动展开与搜索匹配仍按原行为显示内容。
- 修复：页面刷新或会话路由切换后，页面内存 Marker ID 从 `marker-1` 重新开始，持久快照继续复用原 `makerKey`。
- 修复：非 ChatGPT partial DOM 从历史后段挂载时保留缓存分组和 Maker 顺序，新记录按已知锚点插入，不再因局部序号从 0 开始而移动到旧记录之前。
- 版本：修订更新到 `0.42.4(179)`。

- 修复：稳定会话中的回复改用“平台、稳定用户分组、回复序号”派生长期身份；宿主暂缺 assistant ID 时仍可保存历史 Maker，刷新后保留原 `makerKey`。
- 修复：真实消息 ID 作为仅运行时别名参与 v2 快照就地迁移和 DOM 重挂；未挂载活跃消息继续保留，别名冲突、用户归属不明确或多回复局部挂载无法确定序号时保持内存态，避免误映射。
- 修复：恢复 21、25 或 80 个用户分组后仍默认显示最新 20 组，并保留“更早问题”入口；展开与搜索继续显示全部恢复分组。
- 版本：修订更新到 `0.42.3(178)`。

- 性能：AI Maker、用户分组、折叠卡片和浮动 Active Maker 改用基础模糊与饱和度，不再为每个元素生成 SVG 位移及色差滤镜；控制区和搜索框继续保留完整液态玻璃。
- 性能：Maker 类元素退出动态滤镜注册和尺寸监听，活动切换只更新前后两个 class，浮动 Maker 的滚动帧不再计算滤镜。
- 交互：继续使用 Chrome 原生像素级滚动，不修改系统滚轮步长，不增加 wheel 拦截、DPI 倍率或自定义缓动。
- 版本：修订更新到 `0.42.2(177)`。

- 性能：Maker 列表改用浏览器原生滚轮滚动，移除全局 wheel 捕获、透明扩展命中区、逐项宽度读取和自定义缓动动画；20 至 800 个 Maker 的 wheel 合约测试均不触发布局读取或 `preventDefault`。
- 性能：活动 Maker 改由缓存跟踪器只更新前后两个元素，列表滚动帧直接复用当前活动节点；液态玻璃仅在首次注册、尺寸变化或活动状态切换时刷新，不再随普通滚动全量重算。
- 交互：Maker、用户分组和折叠卡片继续支持 4px 阈值拖动，并在 wheel、触控板或拖动开始时取消 1.2 秒自动定位；左侧透明区域不再滚动 Maker 列表。
- 版本：修订更新到 `0.42.1(176)`。

- 功能：豆包、Kimi、千问、元宝和小红书点点迁移到共享 `MakerSnapshotModel`；Maker 使用独立 Key，DOM 仅维护运行时映射，缓存 Maker 的搜索、分组、折叠和未映射跳转与 ChatGPT 使用同一模型。
- 存储：快照升级为按平台隔离的 v2 命名空间，每个平台独立保留七天、20 个会话或 2 MB；无法确认稳定会话或消息身份时自动使用页面内存模式，单个平台存储失败不会影响其他平台。
- 兼容：ChatGPT v1 快照首次打开时惰性迁移，保留原 Maker Key；partial DOM 观察保留未挂载缓存，完整活跃分支继续删除已退出消息。
- 隐私：所有支持平台的本机快照只保存 Maker 标题、最多 160 字符的用户首行预览和定位元数据，不保存回复正文、不参与同步或上传。
- 版本：功能更新到 `0.42.0(175)`。

- 修复：活动 Maker 不再阻止折叠堆叠组或用户问题分组收起；手动收起后保留选中状态，重新展开时恢复活动样式。
- 交互：Maker、用户分组和折叠卡片均可作为列表拖动起点；移动超过 4px 时滚动列表并取消点击，滚轮、触摸板和拖动继续优先于短暂自动定位。
- 版本：修订更新到 `0.41.1(174)`。

- 功能：ChatGPT Maker 改用独立快照模型和稳定唯一 Key；DOM 仅维护运行时映射，旧消息 DOM 被回收后仍保留 Maker 列表，并在点击时按缓存位置尝试重新挂载和精确跳转。
- 存储：Maker 标题、用户首行预览和定位元数据通过 `chrome.storage.local` 在本机临时保存七天，最多保留 20 个会话或 2 MB；不保存回复正文或用户问题全文，不参与 Chrome Sync。
- 稳定性：当前挂载消息更新缓存，活跃分支中的未挂载消息继续保留，退出活跃分支的数据自动删除；存储损坏、超限或不可用时降级为页面内存模型。
- 版本：功能更新到 `0.41.0(173)`。

## 2026-08-27

- 功能：赞赏入口追加固定 Polaris 来源标记；独立赞赏页仅在访客同意后通过 GA4 记录入口到访及视频加载播放按钮点击，不传输 AI 对话内容或扩展设置。
- 版本：功能更新到 `0.40.0(172)`。

- 修复：心形赞赏入口从主导航 Header 移至“关于与设置”面板 Header 右侧，保留原有粉色圆形按钮和新标签页跳转行为。
- 版本：修订更新到 `0.39.1(171)`。

- 功能：Polaris Header 右侧新增心形赞赏入口，点击后在新标签页打开独立赞赏页，并与 Header 内容右边缘对齐。
- 版本：功能更新到 `0.39.0(170)`。

- 发布：原仓库的 `/support.html` 不再承载页面内容，访问时立即跳转至独立 landing 地址。
- 版本：修订更新到 `0.38.8(169)`。

- 发布：赞赏页迁移至独立的 `polaris-landing` GitHub Pages 仓库，正式地址改为 `https://jeffery-ho.github.io/polaris-landing/`。
- 版本：修订更新到 `0.38.7(168)`。

- 设计：赞赏页视频窗口最大宽度限制为 `960px`，使桌面首屏更容易露出 PayPal CTA。
- 版本：修订更新到 `0.38.6(167)`。

- 修复：赞赏页先展示视频缩略图，浏览器确认视频可完整连续播放后才切换至自动播放。
- 版本：修订更新到 `0.38.5(166)`。

- 修复：赞赏页浏览器标签现显示 Polaris 图标。
- 版本：修订更新到 `0.38.4(165)`。

- 设计：赞赏页 PayPal 主 CTA 改为 Apple Blue 液态玻璃质感，保留高对比文字、内高光和悬停浮起反馈。
- 版本：修订更新到 `0.38.3(164)`。

- 修复：赞赏页在视频开始播放前显示加载提示，缓冲时重新提示，并在可播放时显式发起静音自动播放。
- 版本：修订更新到 `0.38.2(163)`。

- 修复：Maker tips 阴影改为贴近本体的轻量弥散投影，移除向下的大范围扩散，最底部提示不再出现明显裁剪。
- 版本：修订更新到 `0.38.1(162)`。

- 功能：新增浅色玻璃风格的 Polaris 赞赏页，提供浏览器扩展、GitHub Issues、介绍视频与 PayPal 直达入口；视频与 PayPal 地址由独立配置集中管理。
- 版本：功能更新到 `0.38.0(161)`。

- 修复：Maker 队列为 tips 预留完整阴影缓冲，悬停提示的底部阴影不再被滚动裁剪。
- 版本：修订更新到 `0.37.6(160)`。

- 修复：导航搜索栏默认状态的占位提示文字改用更高对比度的主题文本色，浅色和深色玻璃背景下均保持清晰可读。
- 版本：修订更新到 `0.37.5(159)`。

- 调整：搜索栏默认沿用主导航的玻璃背景，仅在悬停时显示原有输入背景色。
- 版本：修订更新到 `0.37.4(158)`。

- 调整：Maker 数量折叠分组移除标题前的总数标签，仅保留标题后的余下数量与展开箭头，减少重复信息。
- 版本：修订更新到 `0.37.3(157)`。
- 修复：点击 Maker 后的短暂 Active Maker 定位不再覆盖用户手动滚动；滚轮、触摸板或列表拖动会立即取得控制权，允许 Maker 在流式生成期间保持在列表不可见区域，后续快照不会将其拉回。
- 版本：修订更新到 `0.37.2(156)`。
- 修复：第二组回复生成时，上一组只要已有 Maker 就允许正常折叠；即使 ChatGPT assistant 元数据短暂滞后，也不再错误提示回复尚未加载。
- 版本：修订更新到 `0.37.1(155)`。
- 功能：流式回复改用会话内 Maker 模型缓存与稳定 Key 增量协调；相同 Maker 保留原 DOM，标题原位更新，仅新增、移动或移除真正变化的列表项。
- 交互：生成期间不再清空 Maker 列表或取消滚动动画；列表保持当前视觉锚点并同步修正活动动画目标与拖动基线，允许连续滚动、整体折叠和用户分组折叠，新 Maker 不会自动把队列拉到底部。
- 版本：功能更新到 `0.37.0(154)`。

## 2026-08-26

- 修复：展开最新用户分组时，较晚出现且已有 AI 消息的直接上一组会补充默认展开一次；ChatGPT 会话映射或 DOM 标识短暂不同步时，已加载 Maker 继续保留在原用户分组，不再落入 orphan 或显示为空。
- 版本：修订更新到 `0.36.6(153)`。
- 修复：章节弹窗打开且焦点仍停留在主 Tab 时，普通左右方向键、Home 和 End 不再切换底层“导航 / 章节 / 设置”；既有 `Shift + ← / →` 章节切换和输入框原生编辑行为保持不变。
- 版本：修订更新到 `0.36.5(152)`。
- 修复：普通 AI Maker、用户分组和悬浮 Active Maker 的可见正文统一从胶囊内部左侧开始；用户分组继续靠队列右侧，AI Maker 与折叠卡片继续靠左。
- 版本：修订更新到 `0.36.4(151)`。
- 修复：流式消息每 120ms 重建 Maker 列表时，不再反复播放用户分组、AI Maker、折叠卡片、标题浮层和箭头的悬停过渡；静止状态仍保留原有 hover 动效。
- 版本：修订更新到 `0.36.3(150)`。
- 修复：历史用户 Maker 分组不再把“没有可见 Maker”误判为“AI 回复未加载”；已有 AI 消息的空分组可自由展开或收起，仅确实没有 AI 消息时显示未加载提示。
- 版本：修订更新到 `0.36.2(149)`。
- 修复：流式输出新增最新用户 Maker 分组时，即使 AI Maker 尚未 ready 也会默认展开；同一分组后续重绘与暂时消失再出现不会覆盖用户手动折叠，最新空分组仍可自由展开或收起，历史空分组继续显示未加载提示。
- 版本：修订更新到 `0.36.1(148)`。
- 功能：Maker 输出新增“等待起点 / 渐进渲染”状态机；流式回复出现首个非空有效 Maker 后即开始渲染，后续 DOM 变化按固定 120ms 窗口合并，不再因持续输出反复重置计时器并延迟到回答结束。
- 实现：MutationObserver 复用单次采集的最新 Maker 快照；路由切换和扩展失效会重置状态机，空白标题不再生成临时占位 Maker，输入框与 Polaris 自身 DOM 仍不触发重算。
- 版本：功能更新到 `0.36.0(147)`。
- 修复：缩小模式的 Maker 恢复视口四周安全留白；AI、用户、折叠和悬浮 Maker 标题限制为约 160px 单行，超出部分以省略号显示；用户 Maker 使用不可收缩的 8px 折叠箭头并扩展对应宽度，折叠分组可扩展至 280px，避免箭头被压缩或裁剪。
- 版本：修订更新到 `0.35.8(146)`。

## 2026-08-25

- 修复：全局拖拽后的点击抑制现在明确放行 Maker 按钮，避免 Dia 捕获阶段阻断跳转。
- 版本：修订更新到 `0.35.7(145)`。
- 修复：Maker 列表的按压拖动不再覆盖 Maker 按钮；Dia 中点击时的微小指针移动不会吞掉跳转事件。
- 版本：修订更新到 `0.35.6(144)`。
- 修复：所有 Maker 统一按最近的垂直会话滚动容器计算跳转位置并预留顶部栏；修复 Dia 中列表已读取但普通标题与表格点击后不滚动的问题。
- 版本：修订更新到 `0.35.5(143)`。
- 修复：ChatGPT 重绘表格后，内容完全相同的重复表格 Maker 会优先按原候选序号重新定位；仅当原序号不匹配时才回退到唯一的表头与正文指纹匹配项，避免点击后无响应。
- 版本：修订更新到 `0.35.4(142)`。
- 修复：表格 Maker 纠正为参与 H3 筛选开关；此前错误归入 H2，用户调整 Maker 筛选后可能隐藏表格 Maker，无法继续跳转。
- 版本：修订更新到 `0.35.3(141)`。
- 修复：Maker 筛选触发重渲染而替换表格节点时，表格 Maker 点击会按表头标题、正文指纹与当前文档中的表格候选序号重新获取节点；完全相同的重复表格不猜测跳转目标。
- 版本：修订更新到 `0.35.2(140)`。
- 修复：表格 Maker 点击后优先定位最近的垂直会话滚动容器，并预留顶部固定栏与安全间距；没有内部容器时保留原有 `scrollIntoView` 回退，其他 Maker 的跳转路径不变。
- 版本：修订更新到 `0.35.1(139)`。
- 功能：所有支持平台的可见 AI 回复表格现在会生成一个 Maker；标题取首行至少两个非空单元格并按页面顺序插入，表格整体可作为跳转与章节边界，且不依赖易变的样式类名。
- 版本：功能更新到 `0.35.0(138)`。
- 功能：章节正文可安全解析未被网页渲染的混排 Markdown，支持标题、引用、分隔线、代码、嵌套任务列表、链接与表格中的行内格式；Emoji、中文和其他 Unicode 字符按原样保留，原始图片与 HTML 仍仅显示为文本。
- 版本：功能更新到 `0.34.0(137)`。
- 调整：AI maker 数量折叠分组展开后保持原有玻璃背景；悬停时仅增加同色背景不透明度，不再切换为深灰色高亮底。
- 版本：修订更新到 `0.33.1(136)`。
- 功能：章节正文可识别网页未渲染的完整 Markdown 管道表格，并格式化为可横向滚动的原生表格；列数不一致或分隔行无效的文本保持原样。
- 版本：功能更新到 `0.33.0(135)`。
- 功能：章节标题存在但正文无法提取时，弹窗提供“跳转到原会话位置”；点击后关闭弹窗、恢复页面滚动并定位原章节标题，同时同步右侧 marker 选中态。
- 版本：功能更新到 `0.32.0(134)`。
- 功能：章节正文升级为完整 Markdown 阅读视图，安全保留标题、删除线、嵌套列表、任务列表、图片与复杂表格；图片原比例内嵌，点击后在新标签查看原图，复制仍输出纯文本。
- 版本：功能更新到 `0.31.0(133)`。
- 修复：从设置面板打开更新说明后，点击“我知道了”、关闭按钮、遮罩或 Escape 会返回设置菜单。
- 版本：修订更新到 `0.30.1(132)`。
- 功能：关于与设置面板的版本号旁新增“更新说明”入口，可再次查看当前 `0.xx` 版本说明。
- 版本：功能更新到 `0.30.0(131)`。
- 调整：版本更新弹窗仅展示 `0.xx` 功能版本说明；补丁版本不再单独显示或触发新的更新弹窗。
- 版本：修订更新到 `0.29.4(130)`。
- 调整：版本更新弹窗中的邮件反馈与 GitHub Issue 改用图标按钮表示，保留本地化提示与无障碍标签。
- 版本：修订更新到 `0.29.3(129)`。
- 调整：版本更新弹窗中的更新说明改为按版本倒序展示，最新版本位于最上方。
- 版本：修订更新到 `0.29.2(128)`。
- 修复：版本更新弹窗标题新增小图标和 Emoji 提示；关闭按钮改用居中的 CSS xmark，避免字符视觉偏移。
- 版本：修订更新到 `0.29.1(127)`。
- 功能：新增内置版本更新通知弹窗；首次安装或升级时最多展示最近三个版本的更新说明，并提供邮件和 GitHub Issue 反馈入口。
- 版本：功能更新到 `0.29.0(126)`。
- 修复：豆包新版发送消息气泡改用 `bg-g-send-msg-bubble-bg` 后，Polaris 在内部将其识别为 user，恢复用户 maker 分组；旧版 `send-message-*` 选择器继续兼容。
- 版本：修订更新到 `0.28.6(125)`。
- 修复：未加载回复提示改按 ChatGPT 用户分组当前可渲染的 AI maker 判断，所有点击后无子项的分组都会反馈提示。
- 版本：修订更新到 `0.28.5(124)`。
- 调整：未加载回复的全局玻璃 Toast 改为顶部居中显示。
- 版本：修订更新到 `0.28.4(123)`。
- 调整：未加载回复提示改为全局底部居中的玻璃风格 Toast，跟随页面浅深主题且不拦截操作。
- 版本：修订更新到 `0.28.3(122)`。
- 修复：点击 ChatGPT 会话树中尚未挂载回复 DOM 的历史用户分组时，显示“回复尚未加载”的提示，不再无反馈展开空列表。
- 版本：修订更新到 `0.28.2(121)`。
- 修复：浅色页面主题下恢复用户 maker 的蓝色背景和悬停状态，同时保持 AI maker 的选中黑色状态。
- 版本：修订更新到 `0.28.1(120)`。
- 功能：Maker 列表内部交由浏览器原生惯性滚动，外扩热区改为帧动画累计滚动；导航颜色跟随 ChatGPT 页面视觉主题，浅色页面不再因系统深色偏好显示深灰 maker。
- 版本：功能更新到 `0.28.0(119)`。
- 修复：ChatGPT 长会话的用户 maker 分组改读当前活动会话分支，不再依赖已挂载 DOM；未挂载历史回复不生成不可跳转的 AI maker。
- 版本：修订更新到 `0.27.1(118)`。
- 功能：Navigation 新增可同步的“用户分组上限”设置，默认展示最新 20 个用户 maker 分组；更早分组折叠为可展开入口，搜索不受上限影响。
- 版本：功能更新到 `0.27.0(117)`。
- 修复：用户消息分组即使没有可识别的 AI 标题 maker，也会按对话顺序显示在导航列表中；空分组保留折叠交互但不渲染子项。
- 版本：修订更新到 `0.26.13(116)`。

## 2026-08-24

- Update: The packaged extension name is now `Polaris: AI Chat Navigator — Smart Headings & Quick Jump`.
- Version: Updated to `0.26.12(115)`.
- 修复：构建清单版本不再停留在 `0.26.6(109)`；源清单与构建产物统一为 `0.26.11(114)`。
- 版本：修订更新到 `0.26.11(114)`。
- 修复：章节搜索框聚焦时改回浅灰色底，并限制最大宽度为 360px，避免挤压复制与关闭按钮的间距。
- 版本：修订更新到 `0.26.10(113)`。
- 修复：章节弹窗按可用视口高度收缩；Header、章节 Tab 与 Footer 不收缩，超出的正文在内部滚动，不再裁剪 Tab。
- 版本：修订更新到 `0.26.9(112)`。
- 修复：移除章节弹窗 Header 的最小高度，由搜索框和操作按钮的实际内容自适应撑开。
- 版本：修订更新到 `0.26.8(111)`。
- 修复：章节弹窗不再限制高度；小屏幕内容超出视口时由遮罩层滚动，Header、正文和 Footer 均可正常访问。
- 修复：章节弹窗内按 `Shift + ← / →` 切换章节时，阻止事件传递到右侧主 Tab，主 Tab 保持“章节”。
- 版本：修订更新到 `0.26.7(110)`。

## 2026-08-23

- 调整：评分卡悬停不再改变为深色描边；“立即评分”入口改为黑色，并扩展至与上方内容及关闭按钮对齐的全宽。
- 版本：修订更新到 `0.26.6(109)`。
- 调整：评分卡改为上下排列，说明文案位于上方，绿色全宽“立即评分”入口位于下方并加强视觉层级。
- 版本：修订更新到 `0.26.5(108)`。
- 功能：评分卡新增右上角关闭按钮；关闭状态仅保存在本机，24 小时后在下次打开或重渲染设置页时恢复，重装扩展后重新显示。
- 版本：修订更新到 `0.26.4(107)`。
- 调整：评分卡移动到支持平台说明正下方、设置标题和配置项之前。
- 版本：修订更新到 `0.26.3(106)`。
- 调整：重置配置改为“设置”标题行右侧的小按钮，保留原有重置与同步保存行为。
- 功能：原全宽重置按钮位置改为浅黄色评分卡，提供中英文文案并在新标签打开 Polaris Chrome 应用商店详情页。
- 版本：修订更新到 `0.26.2(105)`。
- 调整：设置 Header 仅保留 Polaris 图标和名称；版本号移至 Footer 左侧，反馈入口保持右侧。
- 功能：设置内容顶部新增中英文支持平台说明，列出 ChatGPT、豆包、Kimi、通义千问、腾讯元宝和小红书点点 AI。
- 版本：修订更新到 `0.26.1(104)`。
- 功能：章节弹窗支持 `Shift + ← / →` 循环切换章节；搜索框等可编辑控件保留原生文本选择，弹窗底部固定展示中英文快捷键提示。
- 版本：功能更新到 `0.26.0(103)`。
- 调整：移除设置面板中的同步状态与状态指示点；配置仍通过 Chrome Sync 在内部读写，Footer 仅保留反馈入口。
- 版本：修订更新到 `0.25.1(102)`。
- 功能：章节正文保留 AI 回复中的 Markdown 语义，支持强调、代码、链接、引用、列表与表格的安全渲染；复制仍输出纯文本。
- 版本：功能更新到 `0.25.0(101)`。
- 修复：章节弹窗正文仅显示当前选中章节；切换章节 chip 后不再混入相邻章节的标题、内容或空状态。
- 版本：修订更新到 `0.24.16(100)`。
- 调整：章节弹窗将“复制当前章节”和“复制全文”合并为“复制”下拉菜单；选择后自动收起，Escape 优先关闭菜单。
- 版本：修订更新到 `0.24.15(99)`。
- 调整：章节弹窗改用明确的白色／近黑色背景，搭配对应描边与阴影，提升正文阅读清晰度。
- 版本：修订更新到 `0.24.14(98)`。
- 调整：降低章节弹窗全局遮罩的不透明度，减轻页面压暗，同时保留弹窗层级。
- 版本：修订更新到 `0.24.13(97)`。
- 调整：章节弹窗直接复用设置面板的玻璃背景、描边、阴影和模糊令牌，亮暗主题下保持统一。
- 版本：修订更新到 `0.24.12(96)`。
- 修复：Checkbox 选中后，勾选框描边同步使用主题高亮色，与外层选中态保持一致。
- 版本：修订更新到 `0.24.11(95)`。
- 调整：统一设置 Slider、Checkbox、重置按钮和联系图标的默认灰色描边，并按亮暗主题适配；悬停、选中与键盘焦点保持高亮。
- 版本：修订更新到 `0.24.10(94)`。
- 调整：移除设置 Slider 轨道和未选中 Checkbox 的默认描边，避免亮色模式出现黑色边框；选中态与键盘焦点保留主题高亮。
- 版本：修订更新到 `0.24.9(93)`。
- 调整：全局矩形控件和章节弹窗统一改用 24pt 圆角；胶囊、marker 和图标按钮继续保持全圆角。
- 版本：修订更新到 `0.24.8(92)`。
- 调整：章节与设置面板的矩形控件统一使用 14pt 大圆角；胶囊、marker 与图标按钮保持全圆角，checkbox 勾选框采用匹配尺寸的 6pt 圆角。
- 版本：修订更新到 `0.24.7(91)`。
- 调整：设置面板外层直接继承顶部 Tab 胶囊的玻璃背景、描边、阴影和模糊令牌，浅色与深色主题的背景风格保持一致。
- 版本：修订更新到 `0.24.6(90)`。
- 修复：slider 拖动期间不再重建设置面板，数值和进度原地更新，释放后再刷新导航并同步保存；同时恢复 Reset 的全宽 Hero 风格按钮基线。
- 版本：修订更新到 `0.24.5(89)`。
- 调整：原生设置面板恢复可见毛玻璃背景，并为 checkbox、slider、重置按钮和联系图标补回 Hero 风格的半透明表面、选中态与焦点反馈；不恢复任何组件库依赖。
- 版本：修订更新到 `0.24.4(88)`。

## 2026-08-22

- 调整：设置面板改为原生 DOM 与 CSS，移除 HeroUI、React、React Aria、Tailwind 与 React Vite 插件依赖；保留毛玻璃视觉、滑块、复选框、同步状态及反馈入口。
- 优化：构建后的内容脚本从约 `777 KB` 降至约 `73 KB`，gzip 后从约 `154 KB` 降至约 `20 KB`。
- 版本：修订更新到 `0.24.3(87)`。
- 调整：顶部第三个 Tab 改为“关于与设置 / About&Settings”，原有配置面板能力保持不变。
- 版本：修订更新到 `0.24.2(86)`。
- 调整：About 面板的 Issue 文档图标替换为 GitHub 标志；原有 Issue 地址、悬停提示、无障碍标签和圆形 HeroUI 按钮样式保持不变。
- 版本：修订更新到 `0.24.1(85)`。
- 修复：小红书点点 AI 优先使用 Markdown 正文识别 assistant 消息，并使用 `.user-message-wrapper` 识别用户消息；主站 `/ai_chat` 未命中时才从不含 Markdown 的 `round-item` 兜底，恢复 user maker 映射。
- 修复：点点标题未能通过正文容器关联时，按会话页面位置归属到最近的前置用户消息，避免 user maker 缺失与 AI maker 落入无归属队列。
- 修复：AI maker 数量折叠卡片改为计数、标题、剩余数量与箭头的固定单行布局；长标题省略显示，不再换行挤压或重叠。
- 调整：章节弹窗背景、搜索框、章节 chips、复制按钮和关闭按钮统一为与设置面板一致的 HeroUI 毛玻璃风格；亮暗主题同步使用 14pt 弹窗圆角、8pt 控件圆角和黑白高亮焦点。
- 调整：章节弹窗保留原生 DOM、搜索、复制与关闭逻辑，仅更新视觉令牌和交互表面。
- 调整：右侧控制胶囊的“设置 / Settings”入口改为“关于 / About”，保留所有既有导航配置能力。
- 功能：配置面板 Footer 新增邮件与 GitHub Issue 图标入口；邮件使用 `mailto:jefferyho.build@gmail.com`，Issue 在新标签页打开项目反馈页，并提供中英文无障碍标签和悬停提示。
- 调整：设置面板宽度与顶部 Tab 胶囊对齐；AI/user maker 和悬浮 active marker 取消固定字符截断，长标题在 Tab 宽度内换行展示。
- 版本：功能更新到 `0.24.0(84)`。
- 修复：maker 类型筛选改用 Shadow DOM 兼容的原生受控 checkbox，H1-H4 与“无序列表”开关可稳定更新 AI maker 筛选；user section 不参与该筛选。
- 修复：Kimi 用户消息选择器收紧为 `.segment.segment-user`，不再用模糊 `segment-user` 类名匹配嵌套容器，避免 user section 重复或摘要混入非正文。
- 修复：设置面板的 marker 类型复选框统一解析为布尔选择状态，恢复 H1-H4 与“无序列表”筛选的即时生效和持久化。
- 修复：用户与 AI marker 改按所属消息容器的会话顺序分组，确保 user marker 后紧跟其对应 AI marker，不再受标题节点 DOM 位置影响。
- 调整：用户 marker 默认表面改为半透明淡蓝玻璃，增加淡蓝描边与内高光。
- 修复：亮色模式的通用 marker 规则不再覆盖用户 marker 的淡蓝玻璃背景；AI 分组宽度改为跟随顶部控制胶囊，避免超出主 Tab 区域。
- 调整：用户 section 与 AI marker 改回既有扁平导航队列的连续行布局，不再渲染额外的对话分组容器。
- 修复：AI 数量折叠卡片使用正确的分组计数，避免达到折叠阈值时发生运行时错误。
- 修复：user section 不再按 DOM 容器顺序置顶排列，而是在保留 AI marker 顺序的前提下插入其关联首项；默认淡蓝玻璃背景进一步变浅，文字改为继承主题默认色。
- 修复：扩展重载导致旧 content script 的 Chrome 运行时上下文失效时，导航会安全断开观察器并移除旧 UI，不再持续抛出 `Extension context invalidated`。
- 修复：设置面板的 React 重渲染改用启动时缓存的图标、版本和路由资源地址，避免扩展重载后再次读取失效的运行时上下文。
- 功能：导航队列新增用户消息分组 marker，覆盖 ChatGPT、豆包、Kimi、通义、元宝和小红书点点；用户消息右对齐并使用淡蓝色高亮，AI 标题 marker 在分组内左对齐。
- 功能：点击用户 marker 可折叠或展开该轮对应的全部 AI marker；最新用户消息默认展开，分组内继续保留原有数量堆叠折叠。
- 调整：导航搜索可匹配用户消息全文或 AI 标题，并自动显示命中的用户分组；章节视图仍只处理 AI 正文。
- 版本：功能更新到 `0.23.0(82)`。
- 功能：设置面板接入 HeroUI React 组件，使用隔离 Shadow DOM 保持毛玻璃亮暗主题，避免影响宿主页面样式。
- 修复：设置面板注入 HeroUI 预编译样式，确保 Card、Slider、Checkbox、Button 等组件样式在 Shadow DOM 中生效。
- 调整：Slider 改为半透明玻璃轨道与主题高亮进度段，避免无轨道状态下仅显示滑块。
- 调整：主 Tab 与设置面板恢复 Polaris 毛玻璃背景，设置 Card 不再显示 HeroUI 默认实心白底。
- 调整：设置 Header 恢复左右排列并移除独立白色背景。
- 调整：Reset 按钮上下内边距调整为 16pt。
- 调整：Sync 状态区域移除独立白色背景。
- 调整：设置 Card 内部容器默认背景统一透明，仅保留外层毛玻璃表面。
- 调整：设置 Header 与 Sync Footer 统一为 8pt 上下内边距、16pt 内容行高和 32pt 模块高度，并移除 Card 默认行间隙。
- 修复：本地开发清单在构建后加载 Vite bundle，不再直接执行含 ES Module 导入的内容脚本源码。
- 版本：功能更新到 `0.22.0(81)`。
- 调整：设置面板改为应用 Header、可滚动配置区和 Sync Footer；三个数值配置改为 Slider，亮色模式统一使用黑色交互高亮。
- 功能：控制胶囊悬停或聚焦时显示圆形最小化/最大化按钮；最小化后仅保留 Voyager 图标和当前 Tab 标题，状态跨支持站点同步保存且不关闭设置面板。
- 版本：功能更新到 `0.21.0 build 80`。
- 功能：marker 队列支持 macOS 与 Windows 触摸板的双指滚动和按压拖动，拖动后不会误触发 marker 跳转。
- 功能：顶部控制胶囊支持整块自由拖拽，位置以 `top/right` 同步保存；搜索、设置和 marker 队列始终跟随同一右边缘，重置后回到默认右上角。
- 调整：设置面板移除 Top gap 和 Right offset；默认位置改为内部固定值。
- 版本：修订更新到 `0.20.1 build 79`。
- 版本：功能更新到 `0.20.0 build 78`。
- 修复：导航仅在检测到可见 AI 回复容器后挂载；AI 容器移除时同步关闭章节视图并恢复页面滚动。
- 修复：章节视图不再因 DOM 更新、状态切换或方向键聚焦而自动弹出，仅响应点击、Enter/Space 或 `Cmd/Ctrl+Shift+F`。
- 修复：章节遮罩关闭时强制隐藏，避免 Kimi 页面样式覆盖默认 `hidden` 规则而自动显示。

## 2026-08-18

- 功能：复制当前章节或复制全文成功后显示“复制成功”toast 提示。
- 版本：功能更新到 `0.19.0 build 77`。
- 调整：章节视图关闭按钮改为圆形玻璃背景的 close 图标。
- 版本：修订更新到 `0.18.5 build 76`。
- 调整：导航下方搜索框跟随 marker 队列展开/折叠状态显示或隐藏。
- 版本：修订更新到 `0.18.4 build 75`。
- 调整：堆叠卡片边缘 padding 与普通文本 marker 保持一致。
- 版本：修订更新到 `0.18.3 build 74`。
- 调整：堆叠卡片左侧新增数量小气泡，显示该组堆叠的 marker 总数。
- 版本：修订更新到 `0.18.2 build 73`。
- 调整：marker 堆叠折叠改为按 `foldThreshold` 分组；仅完整组折叠，尾部保持单条，卡片标题显示组内第一条加剩余数量，并改为垂直向下错位。
- 版本：修订更新到 `0.18.1 build 72`。
- 功能：长 marker 队列新增前 N 条堆叠折叠；默认阈值 20 且可在设置中调整，堆叠卡片显示数量并可原地展开/收起，搜索时自动全部展开，活动项落在折叠区时自动展开。
- 版本：功能更新到 `0.18.0 build 71`。
- 功能：导航 marker 队列与章节视图新增标题模糊搜索；按 Unicode 字符顺序包含匹配并忽略空白与英文大小写，Command/Control+F 聚焦当前搜索框，导航命中跳回标题、章节命中切换弹层正文。
- 版本：功能更新到 `0.17.0 build 70`。
- 功能：新增全局中英文词典；浏览器语言以 `zh` 开头时显示中文，其余语言显示英文并以英文兜底，覆盖全部插件前台文案与 ARIA 标签。
- 版本：修订更新到 `0.16.8 build 69`。
- 调整：加快顶部 Tab 的视觉切换，并避免设置与导航 Tab 切换时全量重新采集标题；“导航”Tab 恢复插件小图标及展开/收起箭头。
- 版本：功能更新到 `0.16.7 build 68`。
- 功能：设置菜单打开后，可按 Escape 或点击胶囊和设置面板外的页面区域退出并返回导航 Tab。
- 版本：修订更新到 `0.16.6 build 67`。
- 调整：深色模式下普通 marker 恢复玻璃背景；仅 hover、选中和悬浮 active marker 使用白底黑字。
- 版本：修订更新到 `0.16.5 build 66`。
- 修复：深色模式 marker 的白色背景覆盖规则移至基础样式之后，确保普通、选中和悬浮 active marker 均实际显示白底黑字。
- 版本：修订更新到 `0.16.4 build 65`。
- 调整：深色模式下普通 marker 改为白底黑字，hover 使用浅灰底黑字，不再显示深色玻璃背景。
- 版本：修订更新到 `0.16.3 build 64`。
- 调整：顶部三 Tab 胶囊、选中 marker 与悬浮 active marker 统一使用主题化选中态：深色模式白底黑字，浅色模式黑底白字。
- 版本：修订更新到 `0.16.2 build 63`。
- 调整：顶部三 Tab 胶囊的选中态改为黑底白字，不影响 marker 的选中高亮。
- 版本：修订更新到 `0.16.1 build 62`。
- 功能：右侧顶部控制区合并为“导航 / 章节 / 设置”三 Tab 玻璃胶囊；导航 Tab 保留 marker 队列的收起与展开，章节 Tab 继续打开全文阅读弹层，设置改为点击显示。
- 修复：通过主世界路由探针监听 SPA 路由切换；离开支持路由时关闭章节视图、恢复页面滚动并移除插件根节点，返回后根据新页面内容重新渲染，避免旧页面 UI 残留。
- 版本：功能更新到 `0.16.0 build 61`。

## 2026-08-17

- 功能：将“爆炸模式”更名为“章节视图”，保留原有快捷键和内部兼容标识。
- 修复：章节 chip 点击后不再被右侧 active marker 覆盖，能够稳定切换选中章节。
- 功能：弹窗内顶部右侧新增“复制当前章节”和“复制全文”，替代“复制选中”。
- 版本：功能更新到 `0.15.0 build 60`。
- 调整：右侧 marker 恢复按文档顺序排列的可滚动队列，项目间距统一为 12pt，并恢复队列滚动和悬浮 active marker。
- 调整：点击 marker 后直接跳转至对应标题，不再滚动到整条 AI 消息顶部。
- 调整：恢复“最大数量”设置项，继续用于限制队列可见高度。
- 版本：修订更新到 `0.14.9 build 59`。
- 调整：右侧 marker 改为跟随映射标题 DOM 的垂直中线，不再使用内部滚动队列。
- 调整：标题离开会话可视区时隐藏 marker；密集标题采用最小避让且不因数量限制隐藏。
- 调整：设置面板隐藏“最大数量”，保留已同步配置以兼容旧版本。
- 版本：修订更新到 `0.14.8 build 58`。
- 调整：恢复右侧 marker 顺序队列与内部滚动，取消 DOM 空间映射。
- 调整：点击 marker 后将所属 AI 消息容器滚到会话顶部，无法解析时回退标题。
- 版本：修订更新到 `0.14.7 build 57`。
- 修复：所属 AI 消息仍可见时，不再隐藏其离屏标题的 marker；离屏标题改为贴靠右侧轨道边缘。
- 版本：修订更新到 `0.14.6 build 56`。
- 修复：marker 不再以整条 AI 回复的中线定位，改为与各自标题 DOM 的垂直中线对应。
- 版本：修订更新到 `0.14.5 build 55`。
- 功能：右侧 marker 改为跟随当前视口内的 AI 消息 DOM；同一消息内的标题围绕消息中线排列。
- 实现：滚动、消息尺寸和 DOM 变化时重新定位 marker，长消息贴边、相邻消息组最小避让，并按“最大数量”优先保留距视口中心最近的 marker。
- 版本：修订更新到 `0.14.4 build 54`。
- 调整：普通 marker 与悬浮 active marker 取消柔和阴影，仅保留一像素 glass 描边。
- 版本：修订更新到 `0.14.3 build 53`。
- 调整：全局导航的 hover 背景统一为“重置配置”按钮的浅灰填充，选中态保持原有强调色。
- 版本：修订更新到 `0.14.2 build 52`。
- 调整：全局导航的 marker、设置、爆炸模式与收起按钮背景统一为设置面板的玻璃背景；hover、选中态及表单控件背景保持不变。
- 版本：修订更新到 `0.14.1 build 51`。

## 2026-07-06

- 功能完善：爆炸模式的 section 边界正式收口到全平台 marker 结果，覆盖 ChatGPT、豆包、Kimi、千问、元宝、小红书点点 AI。
- 实现：爆炸模式不再只依赖原生 `h1-h4` / `role=heading` 查找下一节，而是复用 `collectHeadings()` 的已排序结果切分 section，兼容加粗标题、编号标题、无序列表标题和视频标题 marker。
- 版本：功能完善更新到 `0.14.0 build 50`。
- 功能：新增“爆炸模式”，可把当前 AI 会话里的 assistant 正文汇总到只读全屏文本框中进行全屏选词。
- 实现：在右侧控制区新增“爆炸模式”按钮和 `Cmd/Ctrl+Shift+F` 快捷键，支持 `Escape` 关闭、锁定背景滚动和“复制选中”按钮。
- 版本：功能更新到 `0.14.0 build 49`。

## 2026-06-11

- 修复：tooltip 文本超过当前提示宽度时改为换行展示，不再以省略号截断。
- 实现：保留现有提示宽度配置，允许普通文本换行，并对连续长字符启用断行。
- 版本：修复更新到 `0.13.4 build 48`。

## 2026-06-07

- 修复：在网页版对话框输入时，输入框内的 DOM 变化不再触发 marker 反复重算或变化。
- 实现：MutationObserver 忽略用户输入编辑器和扩展自身 DOM 变化，assistant 容器收集也排除输入编辑器。
- 版本：修复更新到 `0.13.3 build 47`。
- 修复：当过滤后没有可见 marker 时，不再隐藏整个导航控件，保留“收起全部”和“设置”以便重新启用 marker 类型。
- 调整：浅色模式恢复此前的浅白玻璃背景变量，仅暗色模式保留 BoomBranch 风格深色 glass。
- 版本：修复更新到 `0.13.2 build 46`。
- 调整：全局 glass 视觉改为参考 BoomBranch 的亮色/暗色双主题，暗色使用深色半透明卡片、细白描边和柔和阴影，亮色使用白色半透明玻璃。
- 实现：统一 marker、按钮、设置菜单、输入框和 tooltip 的 glass 变量，减少浅白玻璃与暗色按钮混用。
- 版本：修复更新到 `0.13.1 build 45`。
- 功能：全平台无序列表标题 marker 改为独立“无序列表”开关，不再复用 H3 过滤。
- 实现：新增 `enabledUnorderedListByPlatform` 同步配置和 `unordered-list` 来源类型，旧配置默认全平台启用无序列表 marker。
- 版本：功能更新到 `0.13.0 build 44`。
- 修复：小红书主站 `/ai_chat` 点点 AI 页面补齐内容脚本注入和 `xhs-ai-chat-page` 聊天容器识别。
- 版本：修复更新到 `0.12.1 build 43`。
- 功能：小红书点点 AI 页面 assistant Markdown 正文支持扩展到 H4 marker，设置面板在点点平台展示 H4 开关。
- 版本：功能更新到 `0.12.0 build 42`。
- 修复：小红书点点 AI 页面补齐 `www.askdiandian.com` 和 `www.diandianlife.top` 注入范围，避免实际访问域名未注入导致 marker 不生效。
- 修复：点点深度研究页补充 `markdown-styles-deep-research` 容器识别。
- 版本：修复更新到 `0.11.1 build 41`。
- 功能：新增小红书点点 AI 网页支持，assistant Markdown 正文可生成 H1-H3 marker。
- 功能：全平台新增无序列表标题 marker，识别加粗标题、`标题：正文` 和短独立标题，统一作为 H3 marker。
- 版本：功能更新到 `0.11.0 build 40`。
- 修复：默认展开状态下“收起全部”箭头改为朝上，收起后箭头朝下。
- 版本：修复更新到 `0.10.3 build 39`。
- 文档：README 改为 GitHub 展示用的最简版本，补充按平台 Marker 类型过滤、liquid glass 和滚轮命中区优化。
- 版本：文档更新到 `0.10.2 build 38`。
- 调整：设置菜单控件参考 Arco Design 风格，统一 input、checkbox、重置按钮、间距和文字层级。
- 实现：新增 Arco-like 颜色变量，保留 liquid glass 外层菜单，同时让表单控件使用浅灰背景、浅边框、蓝色选中态和轻量 hover。
- 版本：修复更新到 `0.10.1 build 37`。
- 功能：设置菜单新增按当前平台过滤 marker 类型，可独立控制 H1/H2/H3。
- 实现：新增 `enabledLevelsByPlatform` 同步配置，ChatGPT、豆包、Kimi、千问、元宝分别保存启用级别，并防止关闭最后一个级别。
- 版本：功能更新到 `0.10.0 build 36`。
- 优化：marker 列表滚轮命中区域扩大到右侧插件最大宽度，不再需要精确滚到 marker pill 上。
- 实现：新增非点击拦截的 wheel 捕获检测，只在 marker 列表可视纵向范围和右侧插件宽度内转发滚动到列表。
- 版本：修复更新到 `0.9.9 build 35`。

## 2026-06-05

- 调整：设置输入框聚焦态去掉黄色边框。
- 调整：重置按钮去掉玻璃态 1px 描边。
- 版本：修复更新到 `0.9.8 build 34`。
- 调整：设置按钮去掉玻璃态 1px 描边。
- 调整：设置输入框和重置按钮默认背景改为 `#f5f5f5`。
- 调整：设置菜单面板加强 blur、saturate 和玻璃阴影，提升液态玻璃效果。
- 版本：修复更新到 `0.9.7 build 33`。
- 修复：marker 列表内容右边缘和收起按钮右边缘对齐。
- 实现：列表滚动区域改为右侧外扩阴影缓冲，marker 内容边缘保持在导航 root 右侧。
- 版本：修复更新到 `0.9.6 build 32`。
- 调整：设置按钮 hover/focus 颜色改为和收起按钮一致的淡蓝玻璃态。
- 版本：修复更新到 `0.9.5 build 31`。
- 调整：inactive marker 默认背景从灰色玻璃改为白色半透明玻璃，更贴合 liquid glass 视觉。
- 版本：修复更新到 `0.9.4 build 30`。
- 调整：设置菜单面板和设置输入框加入 liquid glass 材质，视觉上和 marker/button 的液态玻璃保持一致。
- 实现：将 `.gpt-paragraph-nav__settings-menu` 和设置输入框加入 per-element filter pipeline，并使用独立玻璃背景变量。
- 版本：修复更新到 `0.9.3 build 29`。
- 修复：右侧 marker 列表滚动容器不再截断 marker 玻璃阴影。
- 实现：为列表滚动区域增加阴影缓冲 padding，并用负 margin 抵消布局尺寸变化。
- 版本：修复更新到 `0.9.2 build 28`。
- 调整：liquid glass 改为参考 `nikdelvin/liquid-glass` 的 data-URL SVG displacement filter 方案。
- 实现：按每个 marker/button 的尺寸和圆角生成 `feImage` + `feDisplacementMap` filter，并用 `ResizeObserver` 在尺寸变化时重算 CSS 变量。
- 版本：修复更新到 `0.9.1 build 27`。
- 功能：玻璃材质新增本地 JS 注入 SVG filter 的 liquid glass 增强。
- 实现：在导航 root 内幂等注入隐藏 SVG filter，使用 `feTurbulence`、`feDisplacementMap` 和 `feGaussianBlur`，CSS 在支持时叠加 `url("#gpt-paragraph-nav-liquid-glass")`，并保留纯 CSS fallback。
- 版本：功能更新到 `0.9.0 build 26`。
- 调整：统一去掉 marker、底部悬浮 active marker 和收起按钮玻璃态上的灰色描边，保留玻璃阴影和模糊材质。
- 版本：修复更新到 `0.8.1 build 25`。
- 功能：marker、底部悬浮 active marker、设置按钮、收起按钮和重置按钮新增纯 CSS 玻璃材质。
- 实现：使用半透明背景、`backdrop-filter` / `-webkit-backdrop-filter`、玻璃边框和内高光，不引入 JS/SVG filter 或第三方库。
- 版本：功能更新到 `0.8.0 build 24`。
- 调整：设置菜单重置按钮 hover/focus 改为参考 marker hover 的浅蓝背景，避免浅色菜单中不可见。
- 版本：修复更新到 `0.7.8 build 23`。
- 修复：点击 marker 或底部悬浮 active marker 后，右侧列表会短时间持续校正到 active marker，避免网页自身滚动或重排打断定位。
- 版本：修复更新到 `0.7.7 build 22`。
- 调整：移除 inactive marker 的位置透明度渐变，默认灰色背景保持一致；hover 和 active 颜色不变。
- 版本：修复更新到 `0.7.6 build 21`。
- 调整：点击底部悬浮 active marker 时保留平滑滚动效果，同时继续先触发正文定位，避免影响一键到位。
- 版本：修复更新到 `0.7.5 build 20`。
- 修复：点击底部悬浮 active marker 时先即时定位正文锚点，再滚回右侧选中 marker，避免需要多次点击才到位。
- 版本：修复更新到 `0.7.4 build 19`。
- 修复：点击底部悬浮 active marker 时，右侧列表滚回选中 marker，并同步把正文页面锚点定位到对应 heading。
- 版本：修复更新到 `0.7.3 build 18`。
- 修复：点击 marker 后 active 蓝色高亮不再因为焦点变化被清除，选中 marker 离开屏幕时固定到可视列表底部上方 20pt。
- 版本：修复更新到 `0.7.2 build 17`。
- 修复：滚动 marker 列表后，点击过的 marker 不再和当前 active marker 同时显示高亮。
- 实现：active 状态改为由点击的 marker 持久驱动，并使用内部唯一 marker key 匹配，避免重复 DOM id 造成多个 marker 同时高亮；active marker 完全离开屏幕可视区时才在可视 marker 列表底部上方 20pt 显示可点击的悬浮副本。
- 版本：修复更新到 `0.7.1 build 16`。

## 2026-06-04

- 需求：将右侧 marker 列表默认位置改为从页面 header 下方开始向下展示。
- 实现：浮层改为顶部对齐，列表高度预留顶部 controls 区域，并移除新增标题后自动滚到底部的逻辑。
- 版本：功能更新到 `0.7.0 build 15`。
- 需求：开始支持千问页面 assistant 正文的 H1-H3 标题导航，并支持千问视频列表首个视频标题 marker。
- 实现：Manifest 增加 `www.qianwen.com` 和 `qianwen.com` 注入范围，内容脚本新增千问 assistant Markdown 容器识别和视频列表首标题收集逻辑。
- 版本：功能更新到 `0.6.0 build 14`。
- 需求：开始支持 Kimi 页面 assistant 正文的 H1-H2 标题导航。
- 实现：Manifest 增加 `www.kimi.com` 和 `kimi.com` 注入范围，内容脚本新增 Kimi assistant Markdown 容器识别，并限制 Kimi 只渲染正文 H1-H2 marker。
- 版本：功能更新到 `0.5.0 build 13`。
- 需求：开始支持元宝页面的 H1-H2 标题导航，并支持元宝视频大卡标题 marker。
- 实现：Manifest 增加 `yb.tencent.com` 和 `yuanbao.tencent.com` 注入范围，内容脚本新增元宝 AI 容器、H1-H2 层级限制和视频卡标题收集逻辑。
- 版本：功能更新到 `0.4.0 build 12`。
- 修复：设置菜单增加透明 hover 判定区域，鼠标从“设置”按钮移向左侧空白区域或下方菜单时不再立刻关闭。
- 版本：修复更新到 `0.3.2 build 11`。

## 2026-06-03

- 需求：将扩展显示名称从 GPT-Voyager/ChatGPT Paragraph Navigator 改为 Polaris for Web。
- 实现：更新 Manifest、README、guideline 标题和运行时 Console/aria 文案，保留内部 DOM/CSS 前缀不变。
- 需求：支持豆包 chat 页面里的 H1-H3 标题导航标记。
- 实现：在扩展注入范围中加入 `https://www.doubao.com/*`，并允许图标资源在豆包页面加载。
- 实现：内容脚本按当前站点自动选择回复区域，新增豆包回复区域选择器，并保留通用 Markdown 容器兜底扫描。
- 调整：标题识别范围统一为 H1-H3，不再记录 H4。
- 需求：让前台设置支持通过 `chrome.storage.sync` 跨设备同步。
- 实现：新增 `storage` 权限，将配置持久化迁移到 `chrome.storage.sync`，并在同步区为空时自动迁移旧 `localStorage` 配置。
- 需求：开始创建并记录 build 号。
- 实现：新增 `builds.md`，使用 `1-999` 数字 build 号记录构建，并在 Manifest 中写入 `version_name`。
- 需求：在设置菜单展示 `chrome.storage.sync` 是否已启用。
- 实现：设置菜单顶部新增同步状态行，读写同步存储成功时显示绿点“同步已启用”，不可用或失败时显示灰点“同步未启用”。
- 规则：每次代码或文档修改都必须新增 build；bug 修复更新版本号最后一位，功能更新更新中间一位，大更新更新第一位；版本类型由用户手动声明，每次 build 后需要确认 build 号和版本号。
- 修复：提高设置菜单层级，避免菜单被右侧 marker 覆盖。
- 需求：在设置菜单中展示当前插件版本和 build。
- 实现：从 Manifest 读取 `version_name` 并在设置菜单中只读展示，用于确认当前加载版本。
- 调整：将版本/build 小字移动到“同步已启用”右侧，间距为 `12pt`。
- 需求：发布到 GitHub、打包 zip，并明确开源协议。
- 实现：新增 MIT `LICENSE`，build 更新到 `7`，准备通过 GitHub Release 发布 `Polaris-for-Web-build-7.zip`。

## 2026-06-02

- 需求：将“收起全部”按钮更新为插件图标、文字和右侧箭头组合。
- 实现：按钮内嵌 32px 插件图标外壳，实际加载 96px 三倍图资源，保留展开/收起文字，并增加右侧 chevron。
- 需求：参考 `Voyager-icon.png` 风格生成可用于 Chrome 扩展商店的标准图标。
- 实现：新增 `icons/gpt-voyager-icon-*` 多尺寸 PNG 图标，主符号使用 `P`，外层画布透明但保留圆角矩形底，并在 `manifest.json` 中配置 16、32、48、128 尺寸。
- 需求：希望不进入扩展程序管理页，也能在 ChatGPT 前台调整导航浮层配置。
- 实现：在“收起全部”左侧新增“设置”入口，鼠标移入或键盘聚焦时弹出配置菜单。
- 实现：支持即时调整顶部间距、右侧间距、最大显示数量和提示宽度，并使用 `localStorage` 持久化。
- 修复：右侧导航容器使用全宽布局，避免 tooltip 被列表宽度裁剪；透明布局区域不拦截正文点击。
